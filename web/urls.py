# from django.urls import path


app_name = "main"

urlpatterns = [
    # Airline
    # path("airlines/", views.AirlineListView.as_view(), name="airline_list"),
    # path("airlines/create/", views.AirlineCreateView.as_view(), name="airline_create"),
    # path("airlines/detail/<str:pk>/", views.AirlineDetailView.as_view(), name="airline_detail"),
    # path("airlines/update/<str:pk>/", views.AirlineUpdateView.as_view(), name="airline_update"),
    # path("airlines/delete/<str:pk>/", views.AirlineDeleteView.as_view(), name="airline_delete"),
]
